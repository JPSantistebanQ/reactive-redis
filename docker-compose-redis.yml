version: '1'
services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"
    #volumes:
    #  - ./data:/data
    #command: redis-server --appendonly yes

  #redis-commander:
  # redis-cli -h redis -p 6379
  # keys *